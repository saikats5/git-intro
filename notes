git config --global user.name saikat
git config --global user.email ss@gmail.com
git config user.name
git config user.email
cd .. --- go back
cd folder --- change dir
ls/dir --- to see list of files
mkdir test --- to create directory
touch test.html --- to create a file//works only on mac or linux
rm test.html --- to remove file
rmdir test --- to remove directory

git init
git status
git add index.html//staging
git rm --cached index.html//unstage
git add .//stage multiple files
git commit -m "Message"//"" double quotes,single quote doesn't work
git log//to see history
git log --oneline//condensed log

git checkout 2866d14//safe online checkout no modification on history
git checkout master

git revert 4e91e97//revert particular commit by adding a new commit
git reset 4e91e97
git reset 4e91e97 --hard//remove from editor

Master branch is the stable branch

git branch feature1
git branch -a//to see all branches
git checkout feature1//to switch the branch
git branch -d feature1//to delete the merged branch
git branch -D feature1//to delete the unmerged branch

git checkout -b feature1//create and checkout new branch

git merge feature1//from the branch where the code needs to be merged
git commit//after merge conflict then shift: and wq to escape the screen

git push https://github.com/saikats5/git-intro.git master

git remote add origin https://github.com/saikats5/git-intro.git

git clone https://github.com/saikats5/git-intro.git

git remote -v//show all alias when cloned from github repo

git pull origin master// fetch all code from master branch in remote to master branch in local

forking and contributing can be done from main account to own account by selecting fork